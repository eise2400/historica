*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/storage/*.key
/storage/pail
/vendor
/database/database.sqlite
Homestead.json
Homestead.yaml
Thumbs.db

# Deploy branch: only public/build/ (compiled Vite assets) is committed on
# purpose, since Node/npm are not confirmed available on the target host.
# vendor/ is installed on the server itself via Plesk's Composer button.
