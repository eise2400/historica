*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/storage/*.key
/storage/pail
/database/database.sqlite
Homestead.json
Homestead.yaml
Thumbs.db

# Deploy branch: vendor/, compiled assets and the package-discovery cache are
# committed on purpose so Plesk's Git integration can pull a ready-to-run
# build without Composer/npm/artisan ever running on the server.
